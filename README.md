# SQL Lib for Golang

Lightweight asynchronous SQL wrapper for Go with MySQL and SQLite support

## Features

- Async execution using goroutines
- Simple API: `ExecSelect` and `Exec`
- Supports both MySQL and SQLite
- Minimal dependencies, easy to integrate

## Installation

Use Go modules:

```bash
go get github.com/SonabaTeam/db
```